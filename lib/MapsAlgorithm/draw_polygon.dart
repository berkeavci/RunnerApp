import 'package:geolocator/geolocator.dart';

// start = the location that ##Activity started.
// destination = the location that ##Activity ended.
createPolyline(Position start, Position destination) async {}
